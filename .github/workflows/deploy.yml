name: 🚀 Deploy automático para o servidor

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Checkout do código
        uses: actions/checkout@v4

      - name: 🔑 Configurar chave SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: 🧪 Testar conexão SSH
        run: ssh -o StrictHostKeyChecking=no deploy@189.126.105.76 "echo ✅ Conexão com o servidor estabelecida com sucesso!"

      - name: 📦 Copiar arquivos para o servidor
        run: |
          rsync -avz --delete \
            -e "ssh -o StrictHostKeyChecking=no" \
            ./ deploy@189.126.105.76:/home/deploy/app/

      - name: 🚀 Reiniciar serviço
        run: |
          ssh -o StrictHostKeyChecking=no deploy@189.126.105.76 << 'EOF'
          cd /home/deploy/app
          docker compose down
          docker compose up -d --build
          EOF
